//A variable can store an integer or floating-pont number
//The following examples show how to declare a variable and initialize it to a number

// These examples show how to declare a variable and initialize it to an integer or floating-point number

let rank = 0;
var passsingScore = 75;

let currentScore = 83.33;

if (currentScore >= passsingScore) {
    rank = 10;
    console.log("You scored " + currentScore + ' points and ranked # ' + rank);
}