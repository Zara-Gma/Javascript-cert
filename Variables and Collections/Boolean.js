// A variable can store a Boolean value (true or false) 
// The following examples show how to declare a variable and initialize it to a Boolean value
// These examples show how to declare a variable and initialize it to a Boolean value

var isWinterSeason = false;
let isColdToday = false;
let isTurnHeaterOn = false;

if (isWinterSeason == true) {
    if (isColdToday == true) {
        isTurnHeaterOn = true;
        console.log('Turn the heater on, please!');
    }
}

console.log(typeof isWinterSeason);